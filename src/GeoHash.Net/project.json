{
  "version": "0.0.1-*",

  "dependencies": {
    "NETStandard.Library": "1.5.0-rc2-24027"
  },

  "frameworks": {
    "netstandard1.5": {
      "imports": "dnxcore50",
      "dependencies": {
        "System.Linq.Parallel": "4.0.1-rc2-24027"
      }
    },
    "net451": { }
  },
  "scripts": {
    "postcompile": [ "dotnet pack %project:Directory% --no-build -c Release" ] // TODO: Upgrade this to use postbuild when that actually gets hit.
  }
}
